[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "newaimultimodal"
version = "0.1.0"
description = "Reference implementation of the NewAIMultiModal blueprint"
readme = "README.md"
authors = [{name = "OpenAI Demo"}]
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://example.com/newaimultimodal"
